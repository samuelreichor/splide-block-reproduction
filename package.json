{
  "name": "splide-block-reproduction",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host=foo.test --port=3000",
    "build": "tsc && vite build",
    "preview": "vite --host=foo.test --port=3001 preview"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@splidejs/splide": "^4.1.4"
  }
}
